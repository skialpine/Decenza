<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppTheme" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <!-- Black prevents colored gap at top on devices where immersive mode
             doesn't fully cover the status bar area (e.g. Lenovo Tab One).
             The app's actual background is rendered by QML Theme.backgroundColor. -->
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <!-- Immersive mode - hide navigation bar -->
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
</resources>
