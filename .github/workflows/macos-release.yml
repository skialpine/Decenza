name: macOS Build

on:
  # Manual trigger from GitHub UI or API
  workflow_dispatch:
    inputs:
      upload_to_release:
        description: 'Upload to latest GitHub release'
        required: true
        default: 'false'
        type: boolean

  # Trigger on version tags (pre-release and release)
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  actions: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-macos:
    runs-on: macos-15

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0  # Full history for version tags

      - name: Bump version code
        if: ${{ startsWith(github.ref, 'refs/tags/v') }}
        id: bump
        run: |
          CURRENT=$(cat versioncode.txt | tr -d '[:space:]')
          NEXT=$((CURRENT + 1))
          echo "$NEXT" > versioncode.txt
          echo "build_code=$NEXT" >> $GITHUB_OUTPUT
          echo "Version code bumped: $CURRENT -> $NEXT"

      - name: Get version info
        id: version
        run: |
          VERSION=$(grep 'project.*VERSION' CMakeLists.txt | sed 's/.*VERSION \([0-9]*\.[0-9]*\.[0-9]*\).*/\1/')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Building version: $VERSION"

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.10.2'
          target: 'desktop'
          arch: 'clang_64'
          modules: 'qtquick3d qtshadertools qtcharts qtconnectivity qtmultimedia qtpositioning qtspeech qtwebsockets qtwebengine qtserialport'
          cache: true
          cache-key-prefix: 'qt-macos-v2'

      - name: Set up ccache
        uses: hendrikmuhs/ccache-action@v1.2.21
        with:
          key: macos-universal
          max-size: 500M

      - name: Configure ccache for CI
        run: ccache --set-config=compiler_check=content

      - name: Create ccache compiler wrappers
        run: |
          # For Xcode generator, use CMAKE_XCODE_ATTRIBUTE_CC/CXX to route
          # compilation through ccache via wrapper scripts.
          WRAPPER_DIR="$RUNNER_TEMP/ccache-wrappers"
          mkdir -p "$WRAPPER_DIR"
          CLANG=$(xcrun -f clang)
          CLANGPP=$(xcrun -f clang++)
          printf '#!/bin/sh\nif [ "$1" = "%s" ]; then shift; fi\nexec ccache "%s" "$@"\n' "$CLANG" "$CLANG" > "$WRAPPER_DIR/launch-c"
          printf '#!/bin/sh\nif [ "$1" = "%s" ]; then shift; fi\nexec ccache "%s" "$@"\n' "$CLANGPP" "$CLANGPP" > "$WRAPPER_DIR/launch-cxx"
          chmod +x "$WRAPPER_DIR/launch-c" "$WRAPPER_DIR/launch-cxx"
          echo "CCACHE_LAUNCH_C=$WRAPPER_DIR/launch-c" >> $GITHUB_ENV
          echo "CCACHE_LAUNCH_CXX=$WRAPPER_DIR/launch-cxx" >> $GITHUB_ENV

      - name: Install Apple Developer ID certificate
        env:
          P12_CERTIFICATE_BASE64: ${{ secrets.MACOS_DEVELOPER_ID_P12_BASE64 }}
          P12_PASSWORD: ${{ secrets.MACOS_DEVELOPER_ID_P12_PASSWORD }}
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          # Create variables
          CERTIFICATE_PATH=$RUNNER_TEMP/developer_id_certificate.p12
          KEYCHAIN_PATH=$RUNNER_TEMP/app-signing.keychain-db

          # Decode from base64
          echo -n "$P12_CERTIFICATE_BASE64" | base64 --decode -o $CERTIFICATE_PATH

          # Create temporary keychain
          security create-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
          security set-keychain-settings -lut 21600 $KEYCHAIN_PATH
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH

          # Import certificate to keychain
          security import $CERTIFICATE_PATH -P "$P12_PASSWORD" -A -t cert -f pkcs12 -k $KEYCHAIN_PATH
          security set-key-partition-list -S apple-tool:,apple: -k "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
          security list-keychain -d user -s $KEYCHAIN_PATH

          # Verify certificate
          echo "Installed certificates:"
          security find-identity -v -p codesigning $KEYCHAIN_PATH

      - name: Resolve latest OpenSSL 3.x
        id: openssl-version
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VERSION=$(gh api repos/openssl/openssl/releases --jq '[.[] | select(.tag_name | startswith("openssl-3.")) | .tag_name][0]' | sed 's/openssl-//')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Using OpenSSL $VERSION"

      - name: Cache universal OpenSSL
        uses: actions/cache@v4
        id: openssl-cache
        with:
          path: ${{ runner.temp }}/openssl-universal
          key: openssl-${{ steps.openssl-version.outputs.version }}-universal-macos

      - name: Build universal OpenSSL
        if: steps.openssl-cache.outputs.cache-hit != 'true'
        run: |
          OPENSSL_VERSION="${{ steps.openssl-version.outputs.version }}"
          OPENSSL_URL="https://github.com/openssl/openssl/releases/download/openssl-${OPENSSL_VERSION}/openssl-${OPENSSL_VERSION}.tar.gz"
          INSTALL_PREFIX="${RUNNER_TEMP}/openssl-universal"

          cd $RUNNER_TEMP
          curl -L "$OPENSSL_URL" | tar xz
          cd "openssl-${OPENSSL_VERSION}"

          # Build arm64
          ./Configure darwin64-arm64-cc no-shared no-tests \
            --prefix="${INSTALL_PREFIX}/arm64" \
            -mmacosx-version-min=12.0
          make -j$(sysctl -n hw.ncpu)
          make install_sw
          make clean

          # Build x86_64
          ./Configure darwin64-x86_64-cc no-shared no-tests \
            --prefix="${INSTALL_PREFIX}/x86_64" \
            -mmacosx-version-min=12.0
          make -j$(sysctl -n hw.ncpu)
          make install_sw

          # Create universal libs
          mkdir -p "${INSTALL_PREFIX}/lib"
          cp -R "${INSTALL_PREFIX}/arm64/include" "${INSTALL_PREFIX}/"
          for lib in libssl.a libcrypto.a; do
            lipo -create \
              "${INSTALL_PREFIX}/arm64/lib/${lib}" \
              "${INSTALL_PREFIX}/x86_64/lib/${lib}" \
              -output "${INSTALL_PREFIX}/lib/${lib}"
          done
          echo "Universal OpenSSL installed at ${INSTALL_PREFIX}"

      - name: Configure CMake
        run: |
          OPENSSL_PREFIX="${RUNNER_TEMP}/openssl-universal"
          mkdir -p build/macos
          cd build/macos
          $QT_ROOT_DIR/bin/qt-cmake ../.. -G Xcode \
            -DCMAKE_OSX_DEPLOYMENT_TARGET=12.0 \
            -DCMAKE_OSX_ARCHITECTURES="x86_64;arm64" \
            -DCMAKE_POLICY_VERSION_MINIMUM=3.5 \
            -DCMAKE_XCODE_ATTRIBUTE_CC="$CCACHE_LAUNCH_C" \
            -DCMAKE_XCODE_ATTRIBUTE_CXX="$CCACHE_LAUNCH_CXX" \
            -DCMAKE_XCODE_ATTRIBUTE_LD="$CCACHE_LAUNCH_C" \
            -DCMAKE_XCODE_ATTRIBUTE_LDPLUSPLUS="$CCACHE_LAUNCH_CXX" \
            -DOPENSSL_ROOT_DIR="${OPENSSL_PREFIX}" \
            -DOPENSSL_INCLUDE_DIR="${OPENSSL_PREFIX}/include" \
            -DOPENSSL_CRYPTO_LIBRARY="${OPENSSL_PREFIX}/lib/libcrypto.a" \
            -DOPENSSL_SSL_LIBRARY="${OPENSSL_PREFIX}/lib/libssl.a"

      - name: Build Release
        run: |
          cd build/macos
          cmake --build . --config Release --parallel

      - name: Bundle Qt frameworks
        run: |
          APP_PATH="build/macos/Release/Decenza.app"
          echo "Running macdeployqt on: $APP_PATH"

          # macdeployqt bundles Qt frameworks and fixes library paths
          $QT_ROOT_DIR/bin/macdeployqt "$APP_PATH" -qmldir=qml -verbose=2

          echo "Bundled frameworks:"
          ls -la "$APP_PATH/Contents/Frameworks/" | head -20

      - name: Sign the application
        env:
          KEYCHAIN_PATH: ${{ runner.temp }}/app-signing.keychain-db
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          APP_PATH="build/macos/Release/Decenza.app"

          # Unlock keychain
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH

          # Sign all nested components first, then the app itself
          # --options runtime enables hardened runtime (required for notarization)
          # --deep signs nested code
          # --force replaces any existing signatures
          codesign --deep --force --options runtime \
            --sign "Developer ID Application: Michael Holm (HKHN2RK2P4)" \
            "$APP_PATH"

          # Verify signature
          echo "Verifying signature..."
          codesign -dvv "$APP_PATH"
          codesign --verify --strict "$APP_PATH"
          echo "Signature verified successfully"

      - name: Create DMG
        run: |
          VERSION=${{ steps.version.outputs.version }}
          DMG_NAME="Decenza_v${VERSION}_macos.dmg"

          # Find the built app (named Decenza.app)
          APP_PATH="build/macos/Release/Decenza.app"
          echo "Looking for app at: $APP_PATH"

          if [ ! -d "$APP_PATH" ]; then
            echo "ERROR: Could not find $APP_PATH"
            find build/macos -name "*.app" -type d
            exit 1
          fi

          # Create a temporary directory for DMG contents
          mkdir -p dmg_contents
          cp -R "$APP_PATH" "dmg_contents/Decenza.app"

          # Create Applications symlink
          ln -s /Applications dmg_contents/Applications

          # Create DMG
          hdiutil create -volname "Decenza" \
            -srcfolder dmg_contents \
            -ov -format UDZO \
            "$DMG_NAME"

          echo "Created: $DMG_NAME"
          ls -la "$DMG_NAME"

      - name: Sign the DMG
        env:
          KEYCHAIN_PATH: ${{ runner.temp }}/app-signing.keychain-db
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          VERSION=${{ steps.version.outputs.version }}
          DMG_NAME="Decenza_v${VERSION}_macos.dmg"

          # Unlock keychain
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH

          # Sign the DMG
          codesign --force --sign "Developer ID Application: Michael Holm (HKHN2RK2P4)" "$DMG_NAME"

          echo "DMG signed successfully"

      - name: Notarize the DMG
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_ID_APP_PASSWORD: ${{ secrets.APPLE_ID_APP_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          VERSION=${{ steps.version.outputs.version }}
          DMG_NAME="Decenza_v${VERSION}_macos.dmg"

          echo "Submitting for notarization..."
          xcrun notarytool submit "$DMG_NAME" \
            --apple-id "$APPLE_ID" \
            --team-id "$APPLE_TEAM_ID" \
            --password "$APPLE_ID_APP_PASSWORD" \
            --wait

          echo "Stapling notarization ticket to DMG..."
          xcrun stapler staple "$DMG_NAME"

          echo "Notarization complete!"

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: Decenza-macOS
          path: Decenza_v*.dmg
          retention-days: 1
          overwrite: true

      - name: Upload to GitHub Release
        if: ${{ inputs.upload_to_release == true || startsWith(github.ref, 'refs/tags/v') }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VERSION=${{ steps.version.outputs.version }}
          DMG_NAME="Decenza_v${VERSION}_macos.dmg"

          # Find the release (by tag or latest)
          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            TAG="${GITHUB_REF#refs/tags/}"
          else
            TAG="v${VERSION}"
          fi

          echo "Uploading to release: $TAG"
          gh release upload "$TAG" "$DMG_NAME" --clobber || echo "Release $TAG not found, skipping upload"

      - name: Cleanup keychain
        if: always()
        run: |
          security delete-keychain $RUNNER_TEMP/app-signing.keychain-db || true

      - name: Warm main branch cache for next release
        if: success() && startsWith(github.ref, 'refs/tags/v')
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="${GITHUB_REF#refs/tags/}"
          IS_PRERELEASE=$(gh release view "$TAG" --json isPrerelease --jq '.isPrerelease')
          if [ "$IS_PRERELEASE" = "true" ]; then
            echo "Skipping cache warm for pre-release $TAG"
          else
            gh workflow run "$GITHUB_WORKFLOW" --ref main || echo "Cache warm dispatch failed (non-fatal)"
          fi
